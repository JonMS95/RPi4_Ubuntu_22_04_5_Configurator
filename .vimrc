set tabstop=4       " Set the width of a tab character to 4 spaces
set shiftwidth=4    " Set the number of spaces to use for each indentation level
set expandtab       " Replace tabs with spaces
set softtabstop=4   " Make the tab key insert 4 spaces (align to a 4-space boundary)
